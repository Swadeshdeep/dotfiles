# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

#autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################

################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,1920x1080@144,auto,1, transform, 0
monitor = ,preffered,auto,1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = pcmanfm
$menu = rofi -show-icons -theme gruvbox-dark-hard -show drun

exec-once = hyprpaper #for hyprpaper aka wallpaper

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox

############################
## ENVIRONMENT VARIABLES ###
############################

# ==============================
# General Display & Scaling Settings
# ==============================

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24

# Wayland scaling
env = GDK_SCALE,1                  # GTK scaling (1 = default, no scaling)
env = GDK_DPI_SCALE,1.25            # GTK font scaling (1.25x for high DPI)

# Qt-specific scaling
env = QT_SCALE_FACTOR,1.25          # Scale Qt applications by 1.25x
env = QT_FONT_DPI,100               # DPI for Qt fonts (100 * 1.25 = 125 effective DPI)
env = QT_AUTO_SCREEN_SCALE_FACTOR,1 # Enable automatic scaling in Qt
env = QT_QPA_PLATFORM,wayland;xcb   # Prefer Wayland, fallback to X11 (xcb)

# ==============================
# Cursor & Theme Adjustments
# ==============================

# GTK Fonts (uncomment and adjust if needed)
# env = GTK_FONT_NAME,Hack Nerd Font, 15
# env = GTK3_FONT_NAME,Hack Nerd Font, 15
# env = GTK4_FONT_NAME,Hack Nerd Font, 15

# ==============================
# Application-Specific Fixes & Scaling
# ==============================

# Telegram (TDesktop) settings
env = TDESKTOP_DISABLE_GTK_INTEGRATION,1 # Avoid GTK integration issues
env = TDESKTOP_FORCE_PANEL_ICON,1        # Ensure Telegram tray icon appears correctly

# Chrome/Chromium settings
# env = CHROME_SCALE_FACTOR,1.25
# env = CHROMIUM_USER_FLAGS,--force-device-scale-factor=1.25
# env = CHROME_USER_FLAGS,--force-device-scale-factor=1.25
# env = GOOGLE_DEFAULT_CLIENT_OPTS,--force-device-scale-factor=1.25
# env = CHROME_WRAPPER,google-chrome-stable --enable-features=UseOzonePlatform --ozone-platform=wayland

# Electron-based apps (VS Code, Discord, etc.)
env = ELECTRON_OZONE_PLATFORM_HINT,wayland
env = ELECTRON_ENABLE_WAYLAND,1
env = ELECTRON_SCALE_FACTOR,1.25

# ==============================
# Additional Overrides & Debugging
# ==============================

# Uncomment if needed:
# env = WINIT_X11_SCALE_FACTOR,1.25    # X11 applications scaling
# env = QT_QPA_PLATFORMTHEME,qt5ct     # Use qt5ct for theming Qt apps
# env = GDK_SCALE,1.25                 # Alternative GTK scaling method


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 0
    gaps_out = 0

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(A0A0A0ff)
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle

   
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 6
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = false

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true
    smart_split = false
    force_split = 2
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    disable_hyprland_logo = true
    disable_splash_rendering = true
    force_default_wallpaper = 0
    vfr = true
    vrr = 0
    mouse_move_enables_dpms = true
    key_press_enables_dpms = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = ctrl:nocaps
    kb_rules =

    follow_mouse = 2

    sensitivity = 0.4 # -1.0 - 1.0, 0 means no modification.
    force_no_accel = true

    touchpad {
      natural_scroll = true
      tap-to-click = true
      drag_lock = true
      disable_while_typing = true
      scroll_factor = 0.4  # Adjust this if scrolling feels too fast/slow
      tap-and-drag = true
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gesture = 3, horizontal, workspace
gesture = 3, up, fullscreen

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device{
    name = telink-k-chimera-mouse
    sensitivity = -0.4

}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, RETURN, exec, $terminal
bind = $mainMod+SHIFT, C, killactive,
bind = $mainMod+SHIFT, M, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod+SHIFT, F, togglefloating,
bind = $mainMod, SPACE, exec, $menu
bind = $mainMod+SHIFT, P, pseudo, # dwindle
bind = $mainMod+ALT, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Added by user, for moving windows within the workspace
bind = SUPER SHIFT, left, movewindow, l
bind = SUPER SHIFT, right, movewindow, r
bind = SUPER SHIFT, up, movewindow, u
bind = SUPER SHIFT, down, movewindow, d
bind = $mainMod+SHIFT, H, movewindow, l
bind = $mainMod+SHIFT, L, movewindow, r
bind = $mainMod+SHIFT, K, movewindow, u
bind = $mainMod+SHIFT, J, movewindow, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# # Example special workspace (scratchpad)
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-
bindel = ,XF86Launch1, exec, rog-control-center
bindel = ,XF86KbdBrightnessDown, exec, asusctl -p
bindel = ,XF86KbdBrightnessUp, exec, asusctl -n

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

#extras
exec-once = swaync # for notifications
exec-once = waybar # for status bar
exec-once = nm-applet # for network management
exec-once = /usr/lib/polkit-kde-authentication-agent-1 # for authentication dialogs
exec-once = pypr
exec-once = hyprpm reload -n
# exec-once = /usr/bin/emacs --daemon &
exec-once = xremap --watch ~/.config/xremap/config.yml
exec-once = gammastep -P -O 3500
# exec-once = hyprpaper #for hyprpaper aka wallpaper

bind = $mainMod, Z, exec, ~/.config/hypr/scripts/waybar-toggle.sh
bind = $mainMod, B, exec, ~/.config/hypr/scripts/waybar-toggle.sh
bind = $mainMod, R, exec, rofi -theme gruvbox-dark-hard -show run
bind = $mainMod, W, exec, rofi -theme gruvbox-dark-hard -show window
bind = $mainMod, T, fullscreen, 1
bind = $mainMod, G, togglegroup
bind = $mainMod+SHIFT, G, moveintogroup
bind = $mainMod+CTRL, G, moveoutofgroup
bind = $mainMod, Tab, changegroupactive
#for clipboard history
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data
bind = SUPER, V, exec, cliphist list | rofi -theme gruvbox-dark-hard -dmenu | cliphist decode | wl-copy
#to make the current app fullscreen
bind = SUPER, F, exec, hyprctl dispatch resizeactive exact 100% 100% && hyprctl dispatch moveactive exact 0 0
# Make all windows in current workspace floating
bind = SUPER, P, exec, ~/.config/hypr/scripts/toggle_floats.sh
#to toggle between windows and focus window on the top
bind = Alt, Tab, cyclenext,
bind = Alt, Tab, bringactivetotop,

#to make second workspace as floating
# windowrulev2 = float, workspace:2
#xwayland config suggested by claude
xwayland {
    force_zero_scaling = true
    use_nearest_neighbor = true
}
#color picker
bind = SUPER, f12, exec, hyprpicker -a -f hex
# Screenshots
bind = , print, exec, ~/.config/hypr/scripts/screenshot.sh full
bind = SUPER, print, exec, ~/.config/hypr/scripts/screenshot.sh region-o
bind = SUPER+SHIFT, print, exec, ~/.config/hypr/scripts/screenshot.sh region
#for swaync-client
bind = SUPER, n, exec, swaync-client -t
# Common window rules
windowrulev2 = float, class:^(pavucontrol)$
windowrulev2 = float, class:^(nm-connection-editor)$
windowrulev2 = float, class:^(org.kde.polkit-kde-authentication-agent-1)$
windowrulev2 = float, title:^(Picture-in-Picture)$
windowrulev2 = float, class:^(firefox)$, title:^(Picture-in-Picture)$

# Resize windows
bind = SUPER CTRL, left, resizeactive, -20 0
bind = SUPER CTRL, right, resizeactive, 20 0
bind = SUPER CTRL, up, resizeactive, 0 -20
bind = SUPER CTRL, down, resizeactive, 0 20
bind = SUPER CTRL, H, resizeactive, -20 0
bind = SUPER CTRL, L, resizeactive, 20 0
bind = SUPER CTRL, K, resizeactive, 0 -20
bind = SUPER CTRL, J, resizeactive, 0 20

# Bluetooth
# exec-once = /usr/lib/bluedevil-daemon
# exec-once = /usr/lib/bluetooth/bluedevil-helper

# Scratchpad Pyprland
bind = $mainMod+SHIFT, RETURN ,exec,pypr toggle term

#for cahnging profile
bindl=,XF86Launch4, exec, bash -c "~/.config/hypr/scripts/asus_profile.sh"
#for batter limit
bind = , XF86Launch3, exec, ~/.config/hypr/scripts/battery-limit.sh

# Auto-start applications on specific workspaces
exec-once = [workspace 1 silent] kitty
exec-once = [workspace 2 silent] zen-browser
exec-once = [workspace 4 silent] spotify
# exec-once = [workspace 4 silent] blanket

#for my keyboard (xremap)
bindl= Alt,f11, exec, bash -c "~/.config/hypr/scripts/keyboard.sh"

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# Remove borders when there's only one window
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = noborder, onworkspace:w[tv1]
# windowrulev2 = noborder, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more

